node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/coverage/

# This project uses pnpm; do not commit npm/yarn lockfiles created locally.
package-lock.json
npm-shrinkwrap.json
yarn.lock

# Operating-system metadata.
.DS_Store
Thumbs.db

/.figma/make/*
!/.figma/make/
!/.figma/make/site.json

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
